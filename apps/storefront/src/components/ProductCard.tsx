"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { AnimatePresence, motion, useReducedMotion } from "framer-motion";
import { ChevronLeft, ChevronRight, Plus, X } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";
import type { CSSProperties, PointerEvent } from "react";
import type { Product, ProductVariant } from "@/types/site";
import { formatPrice } from "@/lib/formatPrice";
import { displayCollectionName, displayMaterial } from "@/lib/productDisplay";
import { useCart } from "@/components/cart/CartProvider";
import ProductImage from "@/components/ProductImage";
import { ProductReviewMini } from "@/components/reviews/ProductReviewMini";

function productImages(product: Product) {
  const base = product.image_urls?.length ? product.image_urls : product.main_image_url ? [product.main_image_url] : [];
  const variantImages = (product.variants || []).map((variant) => variant.image_url).filter((image): image is string => Boolean(image));
  return [...new Set([...base, ...variantImages].filter(Boolean))];
}
function optionNames(variants: ProductVariant[]) { const names: string[] = []; for (const variant of variants) for (const name of Object.keys(variant.options || {})) if (!name.startsWith("__") && !names.includes(name)) names.push(name); return names; }
function exactMatch(variant: ProductVariant, selected: Record<string, string>) { return Object.entries(selected).every(([name, value]) => !value || variant.options?.[name] === value); }
function availableVariant(variant: ProductVariant) { return variant.is_active !== false && variant.stock_status !== "out_of_stock" && Number(variant.stock ?? 1) > 0; }

export default function ProductCard({ product, index = 0, showShortDescription = true }: { product: Product; index?: number; showShortDescription?: boolean; }) {
  const router = useRouter(); const reduceMotion = useReducedMotion(); const { addItem } = useCart(); const href = `/products/${product.slug}`;
  const images = useMemo(() => productImages(product), [product]); const safeImages = images.length ? images : [product.main_image_url].filter((image): image is string => Boolean(image));
  const variants = useMemo(() => (product.variants || []).filter((variant) => variant.is_active !== false), [product.variants]); const inStockVariants = useMemo(() => variants.filter(availableVariant), [variants]); const visibleVariants = inStockVariants.length ? inStockVariants : variants;
  const names = useMemo(() => optionNames(visibleVariants), [visibleVariants]); const needsSelection = names.length > 0 && visibleVariants.length > 1; const unavailable = product.stock_status === "out_of_stock" && inStockVariants.length === 0;
  const [activeIndex, setActiveIndex] = useState(0); const [dragX, setDragX] = useState(0); const [dragging, setDragging] = useState(false); const [pickerOpen, setPickerOpen] = useState(false); const [selected, setSelected] = useState<Record<string, string>>({});
  const pointerId = useRef<number | null>(null); const startX = useRef(0); const startY = useRef(0); const axis = useRef<"x" | "y" | null>(null); const frameWidth = useRef(1); const suppressClick = useRef(false);
  const price = Number(product.price || 0); const compareAt = Number(product.compare_at_price || 0); const hasDiscount = Number.isFinite(compareAt) && compareAt > price && price > 0; const discountPercentage = hasDiscount ? Math.max(1, Math.round(((compareAt - price) / compareAt) * 100)) : 0;
  const material = displayMaterial(product) || "Ruth Istanbul"; const collection = displayCollectionName(product.collections?.name || product.collection_slugs?.[0]) || "Ruth Atelier";
  const selectedVariant = useMemo(() => { if (!visibleVariants.length) return null; if (!needsSelection) return visibleVariants[0] || null; if (!names.every((name) => Boolean(selected[name]))) return null; return visibleVariants.find((variant) => exactMatch(variant, selected)) || null; }, [names, needsSelection, selected, visibleVariants]);
  useEffect(() => { if (!pickerOpen) return; const previous = document.body.style.overflow; document.body.style.overflow = "hidden"; return () => { document.body.style.overflow = previous; }; }, [pickerOpen]);
  useEffect(() => { setActiveIndex(0); setDragX(0); setSelected({}); setPickerOpen(false); }, [product.id]);
  const valuesFor = (name: string) => { const matching = visibleVariants.filter((variant) => names.every((other) => { if (other === name) return true; const value = selected[other]; return !value || variant.options?.[other] === value; })); return [...new Set(matching.map((variant) => variant.options?.[name]).filter((value): value is string => Boolean(value)))]; };
  const move = (direction: -1 | 1) => { setActiveIndex((current) => Math.max(0, Math.min(safeImages.length - 1, current + direction))); setDragX(0); };
  const onPointerDown = (event: PointerEvent<HTMLDivElement>) => { if (safeImages.length < 2 || (event.pointerType === "mouse" && event.button !== 0)) return; pointerId.current = event.pointerId; startX.current = event.clientX; startY.current = event.clientY; frameWidth.current = event.currentTarget.getBoundingClientRect().width || 1; axis.current = null; suppressClick.current = false; setDragging(true); };
  const onPointerMove = (event: PointerEvent<HTMLDivElement>) => { if (pointerId.current !== event.pointerId) return; const x = event.clientX - startX.current; const y = event.clientY - startY.current; if (!axis.current) { if (Math.abs(x) < 7 && Math.abs(y) < 7) return; axis.current = Math.abs(x) > Math.abs(y) * 1.1 ? "x" : "y"; } if (axis.current === "y") { setDragging(false); return; } event.preventDefault(); suppressClick.current = Math.abs(x) > 7; const edge = (activeIndex === 0 && x > 0) || (activeIndex === safeImages.length - 1 && x < 0); setDragX(edge ? x * 0.22 : x); };
  const onPointerEnd = (event: PointerEvent<HTMLDivElement>) => { if (pointerId.current !== event.pointerId) return; const distance = event.clientX - startX.current; const threshold = Math.min(48, frameWidth.current * 0.12); pointerId.current = null; setDragging(false); setDragX(0); if (axis.current === "x" && Math.abs(distance) >= threshold) { suppressClick.current = true; move(distance < 0 ? 1 : -1); } axis.current = null; };
  const trackStyle = { transform: `translate3d(calc(${-activeIndex * 100}% + ${dragX}px),0,0)`, transition: dragging ? "none" : "transform .5s cubic-bezier(.22,1,.36,1)" } as CSSProperties;
  const quickAdd = () => { if (unavailable) return; if (needsSelection) { setPickerOpen(true); return; } addItem(product, 1, selectedVariant); }; const addSelected = () => { if (!selectedVariant) return; addItem(product, 1, selectedVariant); setPickerOpen(false); };
  return (<><style>{`.product-card-root{container-type:inline-size;min-width:0}.product-card-image-frame{position:relative;aspect-ratio:3/4;overflow:hidden;border-radius:18px;background:#fff}.product-card-image-button{width:100%;height:100%;touch-action:pan-y}.product-card-arrow{position:absolute;top:50%;z-index:5;display:grid;width:34px;height:34px;place-items:center;border:1px solid rgba(184,151,106,.26);border-radius:999px;background:rgba(250,247,241,.88);transform:translateY(-50%);backdrop-filter:blur(10px)}.product-card-material-badge,.product-card-sold-out{position:absolute;top:10px;z-index:4;display:inline-flex;min-height:29px;align-items:center;justify-content:center;border:1px solid rgba(184,151,106,.22);border-radius:999px;background:rgba(250,247,241,.9);padding:0 11px;font-size:8.5px;letter-spacing:.14em;text-transform:uppercase;backdrop-filter:blur(10px)}.product-card-material-badge{left:10px;color:var(--gold-dark)}.product-card-sold-out{right:10px;color:#b40016}.product-card-info-row{display:flex;align-items:flex-start;gap:10px;padding:12px 2px 0}.product-card-collection{margin:0 0 4px;color:var(--gold-dark);font-size:8px;letter-spacing:.18em;text-transform:uppercase}.product-card-name{font-family:var(--font-heading);font-size:.91rem;line-height:1.25;color:var(--ink)}.product-card-price-row{display:flex;flex-wrap:wrap;align-items:center;gap:6px;margin-top:8px}.product-card-compare{color:var(--muted-foreground);font-size:10px}.product-card-current{font-size:13px;font-weight:500;color:var(--ink)}.product-card-sale-pill{display:inline-flex;align-items:center;gap:5px;border-radius:2px;background:#b40016;padding:4px 6px;color:#fff;line-height:1}.product-card-sale-pill strong{font-size:11px;font-weight:500}.product-card-sale-pill em{font-size:8px;font-style:normal;letter-spacing:.03em}.product-card-add{display:grid;width:38px;height:38px;flex:0 0 38px;place-items:center;margin-top:10px;border:1px solid rgba(184,151,106,.28);border-radius:999px;background:var(--cream);color:var(--ink);box-shadow:0 4px 14px rgba(33,25,18,.06)}.product-card-root.is-sold-out .product-card-image-frame img{opacity:.72!important;filter:saturate(.82)!important}@container(max-width:190px){.product-card-image-frame{border-radius:10px}.product-card-material-badge,.product-card-sold-out{top:6px!important;min-height:22px!important;padding:0 7px!important;font-size:6.5px!important;letter-spacing:.09em!important}.product-card-material-badge{left:6px}.product-card-sold-out{right:6px}.product-card-info-row{gap:4px;padding-top:7px}.product-card-collection{font-size:6.5px;letter-spacing:.11em}.product-card-name{font-size:7.8px;line-height:1.2}.product-card-price-row{display:grid;gap:3px;margin-top:5px}.product-card-compare{font-size:7px}.product-card-current{font-size:9px}.product-card-sale-pill{gap:2px;padding:3px 4px}.product-card-sale-pill strong{font-size:7.5px}.product-card-sale-pill em{font-size:5.8px}.product-card-add{width:27px;height:27px;flex-basis:27px;margin-top:7px}.product-card-add svg{width:13px;height:13px}.product-card-arrow{display:none}}`}</style><motion.article className={`product-card-root group${unavailable ? " is-sold-out" : ""}`} initial={reduceMotion ? false : { opacity: 0, y: 24 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.1 }} transition={{ duration: reduceMotion ? 0 : 0.56, delay: reduceMotion ? 0 : Math.min((index % 6) * 0.045, 0.2), ease: [0.22, 1, 0.36, 1] }} onMouseEnter={() => router.prefetch(href)}><div className="product-card-image-frame" onPointerDown={onPointerDown} onPointerMove={onPointerMove} onPointerUp={onPointerEnd} onPointerCancel={onPointerEnd}><button type="button" className="product-card-image-button block overflow-hidden" onClick={() => { if (suppressClick.current) { suppressClick.current = false; return; } router.push(href); }} aria-label={`${product.name} ürününü aç`}><div className="flex h-full" style={trackStyle}>{safeImages.map((image, imageIndex) => <div key={`${image}-${imageIndex}`} className="h-full min-w-full"><ProductImage product={product} imageUrl={image} alt={imageIndex === 0 ? product.name : `${product.name} ${imageIndex + 1}`} mode="card" priority={imageIndex === 0 && index < 8} hoverScale={!dragging} className="h-full w-full" fit="contain" /></div>)}</div></button>{safeImages.length > 1 ? <><button type="button" disabled={activeIndex === 0} onClick={(event) => { event.stopPropagation(); move(-1); }} className="product-card-arrow left-3 disabled:hidden" aria-label="Önceki görsel"><ChevronLeft size={16}/></button><button type="button" disabled={activeIndex === safeImages.length - 1} onClick={(event) => { event.stopPropagation(); move(1); }} className="product-card-arrow right-3 disabled:hidden" aria-label="Sonraki görsel"><ChevronRight size={16}/></button></> : null}<span className="product-card-material-badge">{material}</span>{unavailable ? <span className="product-card-sold-out">TÜKENDİ</span> : null}</div><div className="product-card-info-row"><Link href={href} className="product-card-text-link min-w-0 flex-1"><p className="product-card-collection">{collection}</p><h3 lang="en-US" data-latin-uppercase className="product-card-name">{product.name}</h3>{showShortDescription && product.short_description ? <p className="mt-2 line-clamp-2 text-[11px] leading-4 text-muted-ruth">{product.short_description}</p> : null}<div className="product-card-price-row">{hasDiscount ? <><del className="product-card-compare">{formatPrice(compareAt, product.currency || "TRY")}</del><span className="product-card-sale-pill"><strong>{formatPrice(price, product.currency || "TRY")}</strong><em>-%{discountPercentage}</em></span></> : <strong className="product-card-current">{formatPrice(price, product.currency || "TRY")}</strong>}</div><ProductReviewMini productId={product.id}/></Link><motion.button type="button" onClick={quickAdd} disabled={unavailable} whileTap={{ scale: 0.9 }} className="product-card-add disabled:cursor-not-allowed disabled:opacity-35" aria-label={needsSelection ? `${product.name} seçeneklerini aç` : `${product.name} ürününü sepete ekle`}><Plus size={17} strokeWidth={1.45}/></motion.button></div></motion.article><AnimatePresence>{pickerOpen ? <motion.div className="ruth-universal-overlay fixed inset-0 z-[120] flex items-end justify-center p-0 sm:items-center sm:p-6" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={() => setPickerOpen(false)}><motion.div className="ruth-universal-popover max-h-[88dvh] w-full overflow-y-auto px-6 pb-[calc(24px+env(safe-area-inset-bottom))] pt-7 sm:max-w-xl sm:p-9" initial={{y:70,opacity:0}} animate={{y:0,opacity:1}} exit={{y:70,opacity:0}} transition={{duration:.32,ease:[.22,1,.36,1]}} onClick={(event) => event.stopPropagation()}><div className="mb-8 flex items-start justify-between gap-5"><div><p className="text-[9px] uppercase tracking-[0.24em] text-gold-dark">Seçenekleri Belirle</p><h2 className="mt-3 font-heading text-3xl font-normal text-ink">{product.name}</h2></div><button type="button" onClick={() => setPickerOpen(false)} className="grid h-11 w-11 place-items-center border border-gold/25" aria-label="Kapat"><X size={19}/></button></div><div className="space-y-8">{names.map((name) => <fieldset key={name}><legend className="mb-3 text-[9px] uppercase tracking-[0.2em] text-gold-dark">{name}</legend><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{valuesFor(name).map((value) => { const active=selected[name]===value; const possible=visibleVariants.some((variant)=>availableVariant(variant)&&variant.options?.[name]===value); return <button key={value} type="button" disabled={!possible} onClick={()=>setSelected((current)=>({...current,[name]:value}))} className={`min-h-12 border px-4 py-3 text-[10px] uppercase tracking-[0.12em] transition ${active ? "border-ink bg-ink text-cream" : "border-gold/25 bg-ivory text-ink hover:border-gold"} disabled:cursor-not-allowed disabled:opacity-35`}>{value}</button>; })}</div></fieldset>)}</div><button type="button" disabled={!selectedVariant} onClick={addSelected} className="mt-9 w-full min-h-12 bg-ink px-5 text-[10px] uppercase tracking-[.18em] text-cream disabled:opacity-35">Sepete Ekle</button></motion.div></motion.div> : null}</AnimatePresence></>);
}

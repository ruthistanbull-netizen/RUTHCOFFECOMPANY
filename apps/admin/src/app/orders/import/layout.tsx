import type { ReactNode } from "react";
import { ExactOrderImport } from "@/components/base44-exact/ExactOrderImport";

export default function OrderImportLayout({ children: _children }: { children: ReactNode }) {
  return <ExactOrderImport />;
}

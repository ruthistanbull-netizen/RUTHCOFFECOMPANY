import type { ReactNode } from "react";
import { ExactContactMessages } from "@/components/base44-exact/ExactContactMessages";

export default function ContactMessagesLayout({ children: _children }: { children: ReactNode }) {
  return <ExactContactMessages />;
}
